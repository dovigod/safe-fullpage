{
  "name": "safe-fullpage",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "",
  "main": "index.js",
  "scripts": {
    "cache:clean": "npm cache clean --force",
    "lerna": "lerna",
    "build": "pnpm exec nx run-many --target=build",
    "lint": "pnpm exec nx run-many --target=lint",
    "build:react": "nx build react",
    "dev:react": "nx dev react",
    "dev:vanilla": "nx dev vanilla",
    "publish": "lerna publish"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "packageManager": "^pnpm@8.6.0",
  "author": "dovigod",
  "license": "ISC",
  "dependencies": {
    "lerna": "^7.4.2"
  },
  "devDependencies": {
    "nx": "17.1.2"
  }
}
